//import axios from 'axios';

/*axios.interceptors.request.use((config) => {

  config.headers.Authorization = `Bearer 3d8bb05e77c40eb7b85ad25434dc4f6205d79102`;
  
  return config;
},
(error) => {
  return Promise.resolve(error.message);
});*/